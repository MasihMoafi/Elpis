[package]
name = "release-checkpoint-research"
version = "0.1.0"
edition = "2021"
publish = false

# This package is intentionally independent of the repository workspace.
[workspace]

[dependencies]
serde = { version = "=1.0.228", features = ["derive"] }
serde_json = "=1.0.151"
