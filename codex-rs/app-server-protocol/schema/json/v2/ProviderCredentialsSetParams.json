{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Replaces a provider key in the running server only; null restores environment lookup.",
  "properties": {
    "apiKey": {
      "type": [
        "string",
        "null"
      ]
    },
    "provider": {
      "type": "string"
    }
  },
  "required": [
    "provider"
  ],
  "title": "ProviderCredentialsSetParams",
  "type": "object"
}
