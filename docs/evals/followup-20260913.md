# September 13 defect follow-up

Local candidate verification; this is not production acceptance or a published release.

## Short memory citations

The installed runtime reproduced the reported formatting regression: saving a
full evidence ID left it in MEMORY rather than retaining only a short citation.
The runtime control failed with `memory retained long citation IDs` before this
correction (`.tmp/final-candidate/memory-citations-before.log`).

The local persistence layer now assigns stable numeric references and preserves
full IDs in `memory-references/sources.md`. It reserves existing citation numbers,
reuses known IDs, preserves unrelated text, and records original versus saved
memory in the receipt. Provenance is published first; a later failed save may
leave unused references. This is not a multi-file crash-atomic transaction.

Eleven candidate checkpoint/persistence tests passed, including repeated saving,
existing short citations, overlapping evidence indices, unrelated text and the
earlier concurrency controls. Evidence is in
`.tmp/final-candidate/memory-citations-focused-tests.log`. The test build took
8m41s, with a transient measured peak of 79 C despite the 75 C controller setting;
the controller paused and resumed compilation. Raw telemetry is in
`memory-citations-test-build.log` under the same directory.

The production runtime and stripped IDE runtime both passed the extended offline
check: stable short references, retained provenance, original-versus-saved receipt
content, unreadable-source preservation and warnings, plus the existing saving,
restart, malformed-output, manual-edit and queued-input controls. Each run used
18 local requests, including eight fake Luna responses; no paid calls. Logs:
`memory-citations-after-final.log` and `memory-citations-ide-runtime.log` under
`.tmp/final-candidate`. An initial relative-binary-path attempt failed to launch
inside the isolated project; the final run used an absolute path.

The optimized build passed in 5m28s, peak 68 C (`memory-citations-optimized-build.log`).
Installed CLI SHA256:
`bf8df20ac8a94b242d689eeedf0fa3e34cacbf8a8f103a6dd601896f5a64900b`.
Installed IDE runtime SHA256:
`c52175cf36375514e3186ebc080579d5aa7ab31720a6551559a6443b31019100`.
Both match the checked artifacts. CLI version remains 0.2.0. Recovery binaries are
in `~/.local/share/elpis/release-recovery/memory-citations-20260913/`;
restore the CLI with `scripts/install-elpis-binary.sh` using its `elpis-before`
and the installed `~/.local/bin/codex-resources/bwrap`.

Masih must restart/resume with `elpis` to load the correction. Existing running
processes and existing memory are not rewritten by installation. This correction
does not resolve automatic-retention or cross-project semantic failures, and is
not a production-readiness claim or a public release.

| Report | Reproduction and change | Verification |
| --- | --- | --- |
| `/compact 30` reaches 8% during a long task | Custom pressure was checked before user turns, but omitted from tool continuations. Check both boundaries; retain the off setting and native limits. Suppress repeated ineffective pressure compaction. | Installed baseline fails; candidate passes crossing, no-setting, off, and ineffective-compaction controls. |
| Scroll up fails | Composer selection consumed wheel events; small static pagers changed offset without scheduling a redraw. | Native held-selection and small `/usage` wheel checks pass on rebuilt candidate; old runtime fails both reproductions. |
| `/usage` Escape affects conversation | Current candidate closes the pager and receives further text on the same provider stream, including resumed sessions. | Native fresh and resumed controls pass. No new Escape routing change was needed in this batch; Masih's acceptance remains open. |
| Subagents invisible | Footer status disappears during draft editing; resumed history did not restore agent identities. Added a persistent ledger using existing navigation and live event buffers. | Native resumed/busy-draft candidate passes; installed baseline fails. Live lifecycle and replay regressions pass in the full TUI suite. |
| Light Ledger contrast | Failure warnings used pale yellow; expanded details used terminal dimming. Use readable existing light-theme foregrounds. | Inspected native failure and expanded-detail PNGs. Dark palette retained. |
| Stray lines through Ledger heading | Raw history insertion can touch a full-height viewport; invalidation falsely treated existing terminal cells as blank. Repaint unknown cells, including spaces. | Same native fixture now shows clean spaces without stale glyphs; both failure and expanded-detail screenshots inspected. |
| Malformed Smart Prune reply | Request lacked a strict output schema. Send strict compact/unchanged decision shapes; retain parser checks and exact originals on rejection. | Actual runtime compact, unchanged and trailing-malformed controls pass, with schema asserted at the HTTP boundary. Luna accepts the schema in a separate one-call contract test. |
| `/mcp reset` | Added local slash command for the existing reload endpoint. Reload is queued until the next turn. | Offline changed-environment/no-reset control and new-process/tool-discovery check pass; reset makes no model request. |
| Permissions lost on resume | Default launch settings overwrote saved thread permissions. Restore the latest saved policy unless explicitly overridden; retain configuration constraints. | All five server tests and the embedded-resume positive/override control passed. |
| Memory | Added strict saver schema and phase timings; saving and admission remain separate. | Runtime save, restart, disabled, malformed and concurrent-edit controls pass. Luna's unrelated-project lesson probe still fails despite scope guidance. Reliable semantic scoping is not established. |

Raw local evidence is under `.tmp/final-candidate/`: `final-memory-runtime.log`,
`final-mcp-runtime.log`, `held-wheel-candidate/`, `usage-wheel-candidate/`,
`subagent-replay-baseline/`, `subagent-replay-negative/`,
`ledger-light-candidate-20260913-v2/`, and the Smart Prune/pressure runtime logs.
The historical `subagent-replay-baseline` directory contains the successful new
candidate capture; its separate negative directory contains the installed control.
Fixtures use isolated homes and localhost providers unless explicitly identified
as Luna live tests. They do not change real memory or spend on RAG embeddings.

Final native CLI SHA256:
`8d35783c56f23f37102b6b66a1edf0bbfb5f719a6411de48d0b216649f5bc51c`.
Standalone runtime SHA256:
`3e26484fd132d58768fb53040d9717e04e08201ea2602c4e666f0d8dfff40e51`.
The pressure result is `pressure-followup-candidate.log`. Final UI captures are
`held-wheel-final/`, `usage-wheel-final-dismissal/`, `subagent-replay-final/`,
and `ledger-light-repaint-20260913/`. In the 320-pixel usage fixture, dismissal
is checked by restored composer and absent pager; the window is then enlarged
to verify new streaming text. Requiring old history to remain on screen in a
short window was an invalid fixture assumption, not an Escape failure.

The first full suite found that repainting exposed invalid one-row terminal
scroll regions: an MCP warning had been leaking into a blank viewport row.
Tiny history regions now reuse the existing raw insertion path to preserve
history above the viewport. Final native repeats are `held-wheel-history-final/`,
`usage-wheel-history-final/`, `ledger-light-tinyregion-20260913/`, and
`ledger-light-startupwarning-20260913/`; warning screenshots were inspected.
An unrelated account fixture inherited `CODEX_AUTH_HOME`; its exact test passes
with that override unset. Runtime fixtures explicitly isolate their auth home.

Durable offline regressions are `scripts/pressure-compaction-runtime.test.cjs`,
`scripts/smart-prune-runtime.test.cjs`, `scripts/mcp-reset-runtime.test.cjs`, and
`scripts/memory-runtime.test.cjs`, each accepting an absolute standalone-runtime
path. They cover actual request ordering and wire contracts, not only parsers.

Final regression result: **3,207 TUI tests passed, zero failed, five ignored**;
all five saved-permission server tests passed. The TUI suite includes the
embedded permission-resume control, subagent lifecycle/replay, light contrast,
wheel routing and one-row warning preservation. See `final-tui-passed.log` and
`final-resume-permission-tests.log`. The wrapping fixture now reserves actual
history space; the MCP snapshot preserves its warning one row above the composer.
The snapshot's single-space separator is intentional terminal-output data.
New core unit/integration cases were not separately compiled as a core test
binary; actual-runtime fixtures establish the pressure and schema contracts.

Installed locally as `~/.local/bin/elpis` (version 0.2.0, hash above), with the
same checked runtime refreshed inside IDE extension 0.1.20. Stripped IDE hash:
`e112cb8622e114788a7919abdd9197a0ab73452f34fd3f5c7cab21d4bddb6a30`.
Memory and pressure fixtures pass on that stripped artifact. The saved pressure
threshold remains 30. Existing processes require restart to load the new code;
no visible app was opened and no user session was killed.

Rollback binaries are hard-linked under
`~/.local/share/elpis/release-recovery/pressure-ledger-20260913/`, avoiding duplicate
backup storage. Restore the CLI with `scripts/install-elpis-binary.sh` using
`elpis-before` and the existing `~/.local/bin/codex-resources/bwrap`.
No remote push, tag or publication was performed. Memory scope and final user
acceptance remain open; this batch does not establish production readiness.

Memory behavior and its failed scope control are detailed in
[the executed lesson protocol](memory-lessons-review.md) and
[the architecture documentation](../context.md). Timing instrumentation measures
future memory and compaction phases; it does not establish a speed improvement.

## Reported checkpoint conflict

Masih subsequently reported `Memory save failed: checkpoint changed during
consolidation`. Earlier app-server memory fixtures did not run the TUI's separate
end-of-turn checkpoint writer, so their success did not cover this interaction.
The saver holds `memory-save.lock` while consolidating; the terminal writer did
not acquire that lock. An asynchronous previous-turn mirror can therefore change
ES after the next saver reads it. The existing equality check correctly refuses
to overwrite that changed file.

The focused test invokes the actual TUI checkpoint writer while a saver snapshot
is alive, then attempts the save. The correction coordinates ES writers with a
workspace checkpoint lock, skips the redundant terminal mirror while the saver
owns ES, and gives the saver a bounded asynchronous wait if the short terminal
write acquired the lock first. A separate global lock still protects shared
MEMORY, so an unrelated workspace can keep writing its checkpoint. Manual edits
must still reject a stale save.

The separate before-fix test build was cancelled before executing tests; no
failing baseline run is claimed. Source inspection identified the uncoordinated
writers. Candidate checks cover both lock orderings, a different workspace, and
manual edits. The candidate's ten checkpoint tests passed, including the actual
TUI writer collision, reverse lock order, unrelated workspace, and manual edit
controls. The actual-widget animation test also passed: active colors change,
then remain static after turn completion even while another task stays busy.
Evidence: `checkpoint-candidate-tests.log` and `idle-animation-candidate-test.log`
under `.tmp/final-candidate/`. These are focused candidate checks, not user
acceptance. The new core-only lock test was not separately executed.

The successful test build is `checkpoint-idle-candidate-rebuild.log` (5m32s).
An earlier attempt found a missing legacy-client module export; that was corrected
before the passing build. The optimized build completed in 6m03s, peaked at 72°C,
and passed the offline memory runtime controls on both the standalone server and
the stripped IDE runtime. Those controls use local fake providers, not paid Luna
requests. Logs: `checkpoint-idle-optimized-build.log`,
`checkpoint-idle-memory-runtime.log`, `checkpoint-idle-ide-memory-runtime.log`.

Source fixes are committed as `483ec8fd`. Installed CLI SHA256:
`ba9d7704efb737e729c0ff414d1e282bbcdc302e077d734f23da25572fddba62`.
Standalone server SHA256:
`30ab35d238c1de597658b282e7b8e3513d840d6bf1df4dc62d6b86b9ca42e84b`.
Installed IDE 0.1.20 runtime SHA256:
`a25c8ccb9cbd731f82333f0ff2549e2ecc99a1d0074f3307d289ee551ddd47d1`.
The CLI still reports version 0.2.0; hashes distinguish this local correction.

Recovery hard links are in
`~/.local/share/elpis/release-recovery/checkpoint-idle-20260913/`.
No visible app was opened, user session restarted, or remote release published.
Masih restarts/resumes with `elpis` to load this version. After a turn ends, the
name should stay static; active turns still animate, including the current
memory-saving tail. Watch for checkpoint-conflict warnings during ordinary use.
This installation does not close semantic memory scope or production acceptance.
The complete local request checklist is `TASKS.md` at the repository root.

## CLI/IDE history and footer follow-up

The installed IDE history query restricted results to `sourceKinds: ["vscode"]`.
A read-only query against the actual Elpis home returned zero conversations with
that filter, versus ten with interactive sources and all model providers,
including the current CLI conversation. Workspace filtering remains enforced.
The isolated regression failed before the query correction
(`cli-ide-history-before.log`). A CLI fixture with no dynamic tools then exposed
a second failure: resumed chats lacked editor capabilities
(`cli-ide-tools-before.log`). Logs are under `.tmp/final-candidate/`.

The correction supplies validated editor tools on resume, retains the thread ID,
and renders the returned resume transcript. The restored TUI `/ide` implementation
uses the Elpis extension's private IPC endpoint; it does not search Codex's home.
The Full Access footer uses a readable warm foreground on light backgrounds,
preserving its existing effect on dark backgrounds. Twelve IPC/transport checks
passed (`ide-ipc-tests.log`). The restored implementation was adapted from the
local Codex donor at `c4017a87`.

This is persisted conversation handoff, not live chat synchronization. The
existing thread cache and subscriber checks are process-local. Separate CLI/IDE
runtimes do not share ongoing turns or enforce exclusive thread ownership across
processes. Finish the CLI turn and exit cleanly before resuming in the IDE.
Live synchronization remains open in `TASKS.md`; a history-list fix does not close
that requirement.

The first test build took 17m55s (peak 73°C, 1.045s cooling). Footer contrast and
the slash-command check passed. The `/ide` checks initially found three fixtures
using group-writable directories under the workstation's `0002` umask. All sixteen
passed with private directories; the fixtures now explicitly set `0700` to match
the real extension. The unsafe-directory rejection remains intact.

Fingerprint review identified avoidable rebuilds: combined TUI/config test builds
enable config's `tokio-stream/net` and `hyper-util/server-auto` test dependencies
throughout the large TUI dependency graph, unlike the installable build. Routine
`test-build` now selects TUI only; `config-test-build` preserves separate config
coverage. Four fake-compiler build-workflow checks passed, including temperature
guards and separate package selection (`ide-build-workflow-tests.log`).
The inactive debug incremental cache was removed after free space fell to 6.7 GB,
recovering 32 GB. Source, installed binaries, and recovery versions were preserved.

The installable build completed in 12m55s. Updating the build wrapper while it was
running caused its final reporting step to fail after Cargo succeeded; a cached
fresh invocation completed successfully in 2.065s
(`ide-footer-optimized-confirm.log`). This was a wrapper error, not a failed
compiler result. Subsequent review found another test-feature difference:
`insta` enables `similar/inline`, still causing some core/TUI recompilation.
The config split removes the protocol-level duplication, not all rebuilds.

Both actual-runtime history tests passed (`cli-ide-history-candidate.log`),
including CLI-origin discovery, same-ID continuation, editor-tool advertisement,
malformed tool definitions, loaded-thread tool replacement rejection, and foreign
workspace rejection. All 47 Node checks passed against the packaged runtime
(`ide-node-candidate.log`). Ten checks passed in actual VS Code 1.136.1 with the
extracted 0.1.21 package: native CLI `/ide on` included an unsaved editor-only
sentinel; fresh `/ide off` excluded it; the IDE history menu found and resumed the
CLI conversation, invoked `editor_read`, and persisted both clients' messages
under the same ID. The test's CLI exited normally before the IDE resumed it.
No paid provider requests were used.

The Snap/CLI editor launchers exited before testing; invoking the editor's Electron
executable directly passed. Evidence and inspected screenshots are under
`editors/vscode/.test-data/run-1789328351740/`; the summary log is
`ide-native-handoff-electron.log`. Native active-composer light/dark captures are
`footer-active-light/footer.png` and `footer-active-dark/footer.png` under
`.tmp/final-candidate/`. These checks establish the tested behavior, not general
production readiness or simultaneous live synchronization.

### Final candidate verification

The explicit private-directory fixtures passed with the workstation's normal
`0002` umask: 16 IDE-context tests, one rendered footer-contrast test, and one
slash-command test. Logs: `ide-tui-final-tests.log`, `ide-footer-final-tests.log`,
and `ide-slash-final-tests.log`. The separate protocol/server unit additions were
not executed as a full Rust suite; their request behavior was exercised through
the packaged app-server runtime tests.

Aligning the existing workspace `similar/inline` feature with snapshot tests
removed the remaining dependency-feature difference. The installable build then
completed in 33.748 seconds; switching back to the TUI test build took 1.555
seconds and reused the same test executable. Logs: `ide-footer-aligned-build.log`
and `ide-test-cache-confirm.log`. These are warm-cache results, not clean-build
timings. The preceding test rebuild took 18m28s and briefly reached 79°C despite
the 75°C guard, which paused it five times; the guard is reactive, not a hard
temperature guarantee.

The final aligned package again passed all 47 Node checks and 10 actual VS Code
checks (`ide-node-aligned.log`, `ide-native-handoff-aligned.log`). Inspected
handoff screenshot: `editors/vscode/.test-data/run-1789329462024/cli-resumed.png`.
A read-only query of the user's actual current conversation returned 162 turns
in 1.215 seconds (`ide-real-thread-read.log`); the live thread was not resumed.

Native light/dark active-composer captures show readable queue hints. A separate
goal-enabled screenshot probe did not expose the goal footer at that layout and
timed out waiting for it; it is not counted as visual proof. All colored goal
states are covered by the rendered contrast test instead.

Final CLI SHA256:
`0fe8caa4840d2f698c98a4358ec632fc20d60694d3088732d90b2ddd300a0e98`.
Final stripped IDE 0.1.21 runtime SHA256:
`24217085cf09e26e8e4a1363e6eaad82a3b2bf61280cb155bf6edd00d0e0c70d`.
Recovery copies are under
`~/.local/share/elpis/release-recovery/ide-footer-20260913/`.
Installed CLI and IDE runtime hashes match these candidates; installed extension
history, session, and activation sources match the tested source. CLI reports
0.2.0 and extension reports 0.1.21. Restart CLI by typing `elpis` and reload the
VS Code window to activate them. No user application was launched or restarted.

### Shared-runtime observer follow-up

`shared-session.test.js` runs independent clients against one actual app-server
over a loopback WebSocket with an isolated home and local fake provider. The
initial check failed because the observer reported idle during an external turn
(`shared-session-before.log`). A second check failed on attaching during an
already active response (`shared-session-attach-before.log`). Session now tracks
external turn activity, emits other clients' user messages, suppresses local
message echoes, and initializes activity from the resumed turn snapshot.

The check passes for external-message delivery, active attachment, interruption
from the observer, subsequent conversation use, local echo suppression, and an
unrelated-thread negative control. All 48 extension checks passed
(`shared-session-regression-final.log`). No Rust rebuild or paid request was
needed. This is source-level progress, not a new installed extension version.

Live integration remains open: the installed `elpis` entry point rejects
`app-server proxy`, despite that route existing elsewhere in upstream source;
the extension still creates its private runtime. Automatic shared-runtime
connection, routing editor tool requests to their owner, notifications arriving
during attachment, and real simultaneous CLI/IDE UI acceptance require further
work. The test's WebSocket bridge is a fixture, not a shipped transport.

### Attachment ordering follow-up

A deterministic one-write fixture reproduced lost text and stale activity when a
resume response and subsequent notifications arrive in one stdout read
(`ide-attach-burst-before.log`). The RPC reader now yields between messages so
response consumers restore session state before following events. The same
fixture passes (`ide-attach-burst-after.log`), including foreign-thread exclusion
and avoiding duplicate final text.

The webview handoff also dropped events emitted before the replacement session
became active (`ide-webview-handoff-before.log`). Those events are now staged and
delivered after the saved transcript. A failed resume discards staged output and
retains the existing session. All 51 extension checks pass
(`ide-attach-regression-final.log`), including the actual shared-runtime test.

The first real VS Code run resumed the chat but timed out waiting for its status:
the replayed model identity replaced `Conversation resumed`. The inspected
`run-1789330389197/ui-failure.png` shows the resumed transcript. Identity-only
status replay is now suppressed; meaningful turn/error statuses remain.
The final package passed all ten real VS Code checks
(`ide-attach-native-final.log`, `run-1789330498129`).

Connection investigation also verified two additional integration requirements:
the installed TUI inserts Elpis config overrides, making its implicit-daemon
reuse guard false, and upstream's stdio-to-UDS proxy relays raw bytes rather than
adapting the extension's JSON-lines protocol to the daemon's WebSocket protocol.
Neither path provides automatic shared-runtime attachment as currently shipped.

Screenshot inspection raised a possible duplicated user-message concern. The
native handoff check now explicitly requires the submitted message to appear
once. It passed with diagnostic event tracing and again without tracing
(`ide-echo-native.log`, `ide-echo-native-final.log`; final artifacts
`run-1789330775806`). The runtime history test also now executes an editor tool
before asserting a single user echo. No additional rendering patch was justified
by these results; visual acceptance remains with Masih.

Extension 0.1.22 is installed. Its session, RPC, and extension sources match the
checked candidate. The bundled runtime hash remains `24217085cf09e26e8e4a1363e6eaad82a3b2bf61280cb155bf6edd00d0e0c70d`.
This installation includes `4bff4c03` observer behavior and the attachment fixes;
reload VS Code to activate it. CLI installation was unchanged in this follow-up.

### Production local connection transport

The app-server now accepts `--connect SOCKET` to adapt its JSON-lines client
stream to the existing Unix WebSocket backend. It uses the same handshake URL
and message-size limit as the existing remote client, with a five-second connect
deadline. Disconnecting the bridge does not kill the backend. Missing or
incompatible sockets fail instead of creating a replacement runtime. Explicit
listen/configuration options cannot be mixed with connection mode.

The shared-session test now uses the actual runtime bridge, replacing its Node
WebSocket fixture. The previous binary failed this test
(`shared-transport-before.log`); the rebuilt candidate passes both transport
checks (`shared-transport-after.log`) and all 52 extension checks
(`shared-transport-regression.log`). This covers external messages, active-turn
attachment, interruption, local echo suppression, unrelated-thread isolation,
client detachment, and invalid socket/options controls. The incompatible-socket
fixture initially stalled cleanup because its peer did not consume incoming
bytes; it now drains the peer. Leftover test processes from that fixture and an
earlier interrupted shared-session fixture were stopped; no user session was
stopped.

The offline two-job build took 248.234 seconds and peaked at 72°C
(`shared-transport-build.log`). No provider charges or new packages were needed.
This source/runtime candidate is not installed yet. Automatic CLI/IDE startup,
tool ownership, and native simultaneous-client acceptance remain unfinished.
Unix is the currently supported platform for the new bridge.

### September 14: one editor owns dynamic tool execution

The production shared-session test reproduced duplicate execution: both the
original editor and its observing editor executed `editor_documents`
(`.tmp/final-candidate/tool-owner-before.log`). Registered editors now own calls
in attachment order, while all subscribed clients retain conversation events.
Pending calls are not replayed to observing clients. Disconnecting the owner
fails its pending callback instead of replaying a possibly executed operation;
the next registered editor handles subsequent calls. With no registered editor,
the model receives an explicit unavailable-tool response.

The rebuilt production test passes single execution, owner disconnect during a
tool call, later ownership transfer, and a terminal-only subscriber with no editor
remaining. Existing interruption, late attachment, local echo, foreign-thread and
socket-failure controls still pass (`tool-owner-after.log`). All 52 extension
checks pass (`tool-owner-regression.log`). The first build caught an incorrect
enum field name; after correction the offline two-job optimized build passed in
210.740 seconds, peaking at 72°C (`tool-owner-build-retry.log`). No paid provider
requests were made. This is runtime evidence, not native simultaneous UI acceptance.

The normal-launch integration remains unfinished and this candidate is not
installed. A new direct launcher check also confirms Elpis currently rejects
`--remote` (`cli-remote-before.log`); the underlying TUI library's remote support
does not expose that flag in Elpis's actual entry point.

### Existing-socket discovery and real simultaneous terminal + VS Code

The launcher now exposes `--remote` and passes its parsed endpoint to the TUI and
conversation operations. The optimized launcher-only build passed in 26.477s,
peaking at 63°C (`cli-remote-build.log`). IDE chat and history reuse the default
local socket when present; a present invalid socket does not silently select a
private runtime. The production discovery test failed before this change
(`shared-discovery-before.log`) and passes afterward. All 52 extension checks pass
with the actual terminal included (`shared-launch-regression.log`).

Real VS Code acceptance passed with a simultaneously running terminal in isolated
Xvfb (`shared-ui-native-retry.log`, run-1789332804447). The IDE shows a terminal
user message once, observes its active turn, interrupts it, serves unsaved editor
text to a terminal-origin tool request, and then sends a follow-up visible in the
terminal. The same thread retains both responses. The light-theme screenshot
`simultaneous-cli-ide.png` was inspected; the transcript is visible. A VS Code Git
notification partly covers the bottom of the composer in this capture.

Two fixture corrections were necessary: use a non-migrated fixture model for the
terminal, and reconnect the already-open IDE panel after the test changes its
home/runtime settings. The first IDE attempt used its original isolated home and
reported reconnect retries instead of reaching the controlled provider; it was
not a passing sync run. The successful run reaches the intended local provider.

Normal server startup is still not automatic. These checks explicitly start the
shared server and use `--remote` for the terminal; they do not establish that typing
`elpis` alone enters the shared server. This candidate remains uninstalled.

### Automatic startup and confirmed permission updates — September 14

Normal Unix CLI startup now starts/reuses the private shared backend. IDE history
and chat use `--shared`, including stale-socket recovery; explicit transports and
CLI configuration overrides retain their separate behavior. A loaded CLI thread
can receive its first editor tool definitions for subsequent turns. Existing
nonempty, incompatible tool definitions are still rejected.

The offline optimized build passed in 385.900s, peaking at 75°C with one thermal
pause (`automatic-shared-build.log`). The ordinary CLI-first check creates its
own daemon, submits a message, discovers that same conversation from IDE history,
attaches editor tools while the terminal remains open, and receives the IDE reply
in the terminal. The real VS Code/terminal run also passed with plain CLI startup
(`automatic-ui-native.log`, run-1789333851143); this UI fixture prestarts its server.
The screenshot was inspected and shows a readable transcript; the Git notification
still partly covers the composer. The CLI-first test separately covers autostart.

Resuming in the IDE preserves actual runtime permissions. Changing the permission
preset now updates the running thread and waits for its settings notification;
the queued acknowledgement alone does not announce success. Static editor
instructions no longer embed a permission description that becomes stale.
Controls cover queued-only acknowledgements, unrelated notifications, disconnect,
and actual Ask/Auto/Full transitions visible to another attached client. All 54
extension checks pass with the actual CLI/runtime and automatic mode enabled
(`automatic-reviewed-regression.log`), with no skipped tests.

This candidate is **not installed**. Review identified one unresolved regression:
provider keys saved in the IDE are inherited by the bridge, but an existing shared
daemon retains its earlier environment. A safe credential handoff must be resolved
before installing automatic sharing. No credentials have been moved into config
files and no paid provider evaluation was run for these checks. Full production
acceptance, including memory quality, remains open.

### Reviewed shared runtime installed — September 14

Commits `b3240869` and `543a1dc6` complete ordinary Unix shared startup
and credential handoff. IDE credentials now reach an existing daemon through a
validated, redacted RPC. OpenAI uses the effective authentication manager so the
endpoint and authentication mode agree. Credentials are shared within that local
provider runtime, not isolated by thread. The CLI also preserves a separate
inherited authentication home when spawning its daemon.

Both regressions were reproduced before fixing them (`shared-credentials-before.log`
and `shared-auth-home-before.log`) and pass in `shared-auth-after.log`. The final
offline optimized build passed in 253.405s at a peak of 70°C. An earlier build
caught a dependency incorrectly limited to development; two other builds were
stopped deliberately during authentication review and build-flag correction.
Five build-guard checks pass, and generated protocol fixtures compare equal after
normalizing JSON key order. Full Rust unit suites were not run in this batch.

The exact packaged runtime passed **55 extension checks, none skipped**, and
**21 native VS Code checks**, using a controlled localhost provider, with no paid
inference. Evidence: `shared-packaged-regression.log`, `shared-packaged-native.log`,
and `editors/vscode/.test-data/run-1789336004359/`. The simultaneous CLI/IDE
screenshot was inspected: transcript text is readable; a VS Code Git notification
partly obscures the composer. Tests cover shared history, interruption, both
message directions and unsaved editor tools. CLI-first checks separately verify
automatic startup and preserved authentication.

Installed CLI SHA-256:
`26b3ef503e9f47fdf487778445dcf291f37d44acb3d22dbe66dc2a754fe8565d`.
Installed extension **0.1.23** runtime SHA-256:
`908386331d00a2f749a678a9733bb1c18a0ac6bdc2a093cfb79dc4c916392275`.
Installed runtime files match the reviewed package byte-for-byte; the manifest
matches after excluding VS Code's added installation metadata and formatting.
CLI version remains 0.2.0.
Rollback copies of the previous CLI and extension 0.1.22 are retained under
`~/.local/share/elpis/release-recovery/shared-cli-ide-20260914/`.
No visible app was opened, user session stopped, or remote release published.

Start a fresh `elpis` process and reload VS Code to activate these changes.
Explicit CLI configuration overrides/profiles can still use a private runtime;
existing processes do not acquire the new code in place. Memory quality, broad
daily-driver acceptance, and proof of all original worktree features remain open.
# September 14: installed versus running recheck

Installed CLI captures also cover `/plan` during an active response with a draft:
`enter to queue message · Plan mode` is readable on white and dark backgrounds.
Both screenshots were inspected. Evidence:
`.tmp/final-candidate/footer-current-report-{light,dark}-short/` contains PNG,
terminal text and results. One controlled local-provider request per theme;
no production source edit was justified by this reproduction. This covers Plan,
not every label or terminal theme transition. Earlier queue-only captures did
not establish colored-label visibility; their goal-label captures failed.

The renewed report of missing `/ide`, invisible footer text and missing IDE
conversations was checked against actual processes. All three existing CLI
processes still mapped the replaced `ba9d7704` executable, predating the footer,
`/ide` and shared-startup changes. The installed executable was `dd8be96d` and
VS Code's registry selected extension 0.1.26. No user process was terminated.

The installed CLI and installed extension runtime passed ordinary automatic
shared-startup, History discovery, live editor-tool attachment and bidirectional
response visibility (`current-installed-shared.log`). An isolated actual VS Code
1.137.0 run passed 23 checks, including terminal `/ide on` delivering unsaved
editor context, `/ide off` excluding it, History finding the CLI conversation,
resume and live editor tools. The light-theme resumed-chat screenshot was
inspected. Evidence: `editors/vscode/.test-data/run-1789341607364/result.json`,
`cli-resumed.png`, and `current-installed-native-electron.log` under
`.tmp/final-candidate/`. These used a local controlled provider, not paid inference.
The Snap launcher exited without running checks; a nonexistent conventional
Electron path also failed. Only the direct installed Snap Electron run is counted.

Limits: History requires an identical workspace directory; opening the IDE panel
does not automatically select the active CLI thread. `/ide` is a terminal command
for editor context, not an IDE-composer sync command. Already-running processes
do not upgrade in place. Restarting the CLI normally and reloading VS Code are
required to test the installed candidate. The user's actual footer report remains
open until the relevant label is visible and accepted in both themes.

## Durable close failure recovery

Archive review identified a live failure path in `AgentControl::close_agent`:
the loaded-agent branch logged a failed Closed-edge write and proceeded with
shutdown. A real local graph-store fault-injection test failed because close
returned a successful operation ID despite that error. Source `af026a24` returns
the persistence error before teardown. The final fixture creates an idle child
using non-triggering communication and proves the same runtime and metadata stay
loaded with an Open edge, then disables the fault and verifies a retry unloads the
child and persists Closed. All 51 agent-control tests pass, including descendant
shutdown and closed-descendant resume controls.

Evidence under `.tmp/final-candidate/`: `durable-close-before.log`,
`durable-close-control-tests.log`, and the corresponding before/after build logs.
The baseline core test build took 1,046.556 seconds, peak 75°C; the corrected
build took 422.575 seconds, peak 76°C. Both used two Cargo jobs, one compiler
frontend thread and the 75°C thermal guard; the second observed a brief overshoot.
The five build-guard tests also pass. Builds emitted existing warnings.

An actual app-server regression then reproduced the separate close-tool event
reporting Completed on failure (8 of 9 controls passed). The handler now marks
the event Failed when closing returns an error. The same nine controls pass:
the model receives the persistence error, the child stays loaded with an Open
edge, and retry after removing the SQLite fault completes and persists Closed.
The permanent regression is `editors/vscode/test/durable-close.test.js`, using a
localhost synthetic provider and isolated state, with no paid inference. Raw
before/after evidence is `/tmp/elpis-durable-close-cSV39G/result.json` and
`/tmp/elpis-durable-close-63wF2o/result.json`. The final optimized build took
40.884 seconds, peak 76°C with the same 75°C guard.

Limits: this does not add archived authority snapshots, exact-edge readback,
Open-write rollback or missing-edge rejection. The 51 controller checks preceded
the event-only patch; the actual-handler regression exercises the final runtime.

Installed local candidate: CLI SHA-256
`a7c4376721d24681c7c8846719c70e9a9eb5dfb7febc533cd9067193328726cc`,
extension0.1.27/runtime
`b114ed5d499fa16752776dca514f8f3ec82817d62337b52a39886880db450e22`.
All56 packaged extension checks pass, none skipped. All42 installed files and
the normalized manifest match the package; registry selects0.1.27. Both installed
automatic shared-start and durable-close controls pass (`durable-close-installed-tests.log`).
Artifacts contain no personal build path. The previous CLI and extension are
preserved at `~/.local/share/elpis/release-recovery/durable-close-20260914/`;
restoring those local files provides rollback. No user session was stopped and
no visible application was opened. One main worktree remains; unrelated
`docs/USER_REQUESTS.md` edits remain untouched. This is a local candidate, not a
production-readiness or user-acceptance claim.

## Spawn and resume persistence failures — September 14

The installed0.1.27 runtime reproduced two additional faults using isolated
SQLite triggers and a localhost synthetic provider. Failed Open writes during
resume returned success, left the child loaded with a Closed edge, and prevented
a later retry from repairing that edge. Four of twelve controls failed
(`/tmp/elpis-durable-resume-kOj1r1/result.json`). Failed fresh spawn left an
untracked child loaded and actually started its inference; five of nine controls
failed (`/tmp/elpis-durable-spawn-Wbra6N/result.json`). No paid inference was used.

The candidate makes Open persistence return an error and checks it before
committing reserved capacity, notifying subscribers, starting completion watchers
or sending initial input. On failure it shuts down and removes the newly loaded
runtime. Direct shutdown avoids a separate rollout flush preventing cleanup.
Uncommitted reservation guards release capacity and paths. The permanent runtime
fixture covers all three lifecycle operations and limits child capacity to one
so retry also checks reservation recovery.

This preserves failed-attempt rollout evidence; it does not undo lifecycle events
already emitted during loading, a consumed nickname, or an earlier V2 eviction.
Missing-edge rejection and the broader archived authority protocol remain separate.
The optimized build passed in228.009 seconds, peak72°C, with two Cargo jobs,
one compiler frontend thread and the75°C guard. All three runtime tests pass
(30 internal controls), including retry with a single available child slot.
Evidence: `durable-open-build.log`, `durable-open-runtime-tests.log` under
`.tmp/final-candidate/`; corrected raw results are
`/tmp/elpis-durable-resume-5lWc2e/result.json` and
`/tmp/elpis-durable-spawn-m76FU5/result.json`. Existing compiler warnings remain.
Full core unit suites were not rebuilt for this patch; actual-handler controls
exercise the final runtime. All58 packaged extension checks pass, none skipped;
installed shared-start plus lifecycle tests pass4/4. All42 installed files and
normalized manifest match the package, and VSCode registers extension0.1.28.
CLI SHA256 `b2af646fc0de5e8d40c42bf236add05cf0dffb1f32ad5a3a8d6a72cc1ade40fb`;
IDE runtime `6e9e853920adca800e6ad9c3ae741b025f576f9b9abb26c8e986bb9d8647381d`.
Neither artifact contains personal build paths. Logs: `durable-open-package-tests.log`
and `durable-open-installed-tests.log`. Previous installed files are preserved at
`~/.local/share/elpis/release-recovery/durable-open-20260914/`. No user session was
stopped, visible app opened or remote release published. Memory policy remains
unchanged while the explicit-global versus automatic-global decision is pending.

## Renewed footer and IDE report — September 14

A read-only inspection after installing 0.1.29 found three live CLI processes
(12575, 13226, 14851) still executing the deleted older `ba9d7704` binary. The
shell resolves the current installed `20157b67` binary; replacement does not
upgrade those running processes. VS Code's extension registry selects 0.1.29,
but registration alone does not prove an existing extension host reloaded it.
The normal `~/.elpis/app-server-control/app-server-control.sock` was absent.
The only observed app-server used an isolated native-test home, not the user's
normal shared runtime. No processes were stopped or visible applications opened.

History requires an exact project directory in both the server request and
client filter (`editors/vscode/src/history.js`). The old CLI processes' OS cwd
was `/home/masih/Desktop/p`; recorded editor workspaces included its Elpis child
folder. This is a possible additional mismatch, not proof of the current thread
cwd or active editor folder. Test with a fresh `elpis` in the same folder opened
in VS Code, reload the extension, and select the CLI conversation in History.
The CLI `/ide [on|off|status]` controls editor context; History selects the chat.

The coordinator inspected the retained native Plan-footer screenshots in both
themes (`footer-current-report-{light,dark}-short/footer.png`); the colored Plan
label beside the queue hint is readable. This does not establish all theme
transitions or acceptance in the user's terminal. The repository's standing
Contrast rule explicitly covers secondary labels, footer and ledgers.

### Vim footer contrast

A hidden native VTE run of installed CLI `20157b67` reproduced bright ANSI green
for `Vim: Insert` against a white background. The draft and neighboring text were
readable; the label bypassed the existing theme-aware accent used for Normal mode.
Evidence: `.tmp/final-candidate/vim-footer-light-draft/insert.png` and its
`result.json` (zero provider requests). The first fixture timed out with its long
isolated home path; a second incorrectly waited for a Normal label hidden by the
empty-composer hint. Neither failed setup is counted as a contrast result.

The change gives Insert the existing theme-aware accent and updates existing
mode-transition expectations. All 65 Vim tests and the existing light/dark footer
contrast test passed. Native candidate captures were inspected in both themes:
`.tmp/final-candidate/vim-footer-{light,dark}-after/insert.png`; both fixtures
passed with zero provider requests. The light label is dark gold and the dark
label bright gold. This is a reproduced footer defect, not proof that Vim was
enabled in Masih's original report. Unknown-palette fallback and theme-transition
coverage remain open.

The test build took 401.722 seconds, peak 73°C; the optimized installable build
then took 5.177 seconds, peak 68°C. Both used the two-job, one-frontend-thread,
75°C-guard workflow. Logs: `vim-contrast-{test-build,tests,footer-tests,build}.log`
under `.tmp/final-candidate/`. The installed CLI matches the captured binary:
`2312f4c291e65ec18f2635f03794a3003cea922c641dcd3ac420fc9e2b9a2c85`.
The prior CLI is retained at
`~/.local/share/elpis/release-recovery/vim-contrast-20260914/elpis`.
Extension 0.1.29 remains installed; this patch changes terminal styling only.
No user process was restarted or remote release published.

### Missing terminal color replies

Native VTE with a PTY relay withholding only OSC10/11 queries reproduced pale
gold footer and Context Ledger labels on white with installed CLI `2312f4c2`.
The relay recorded two dropped queries; the fixture made zero provider requests.
Evidence: `.tmp/final-candidate/missing-palette-light-before/insert.png` and the
isolated home's `palette-queries-dropped.json`. Other terminal negotiation and
ordinary input/output passed through the relay.

The candidate uses terminal-default foreground for shared brand styles, motion
labels and both Ledger palettes when background colors are unknown. Motion spans
explicitly reset foreground to avoid inheriting a colored parent; a Paragraph
regression covers this. Known light/dark palette calculations are preserved.
Decorative borders and streaming reveal behavior are separate from this text
fallback. All 156 relevant style, motion, ledger, footer and Vim checks pass in
both default and forced-truecolor runs. One manual motion-export test is ignored;
it is not counted as passing. The expanded palette test initially rejected the
existing black-on-light low-color fallback; its expectation was corrected without
changing that production behavior. Logs: `missing-palette-{checked,truecolor}-tests.log`
under `.tmp/final-candidate/`.

Four native captures passed and were inspected: `missing-palette-{light,dark}-after`
and `known-palette-{light,dark}-after`, each containing `insert.png` and
`result.json`. The missing-palette cases each recorded two withheld color queries.
All four made zero provider requests. Missing-palette labels use terminal black
or white; known-palette labels retain their existing gold colors. These fixed
states do not prove theme transitions or all animation phases.

The final test rebuild took 105.585 seconds, peak 72°C; the optimized build took
5.179 seconds, peak 68°C, both under the existing guarded build workflow.
Installed CLI SHA-256:
`ffc524eea7076fd5b74d318086958602783540814f5d6ca158e09ac42695af6f`, identical to
the captured candidate. Previous CLI:
`~/.local/share/elpis/release-recovery/missing-palette-20260914/elpis`.

### Resumed Usage dismissal

Installed CLI `ffc524ee` passed an actual native resume check: create a saved
conversation, exit the seed terminal gracefully, restart with its saved ID,
verify the seed in the transcript viewer, and start another response. Open
`/usage`, scroll its small viewport, then press Escape. The usage pager closes,
the same provider response remains open, and a new streamed marker appears.
The continued-response screenshot was inspected. Two controlled local requests;
no paid calls or user sessions touched. This does not exercise an active goal.
Evidence: `.tmp/final-candidate/resumed-usage-transcript/result.json`,
`resumed-history.png`, `continued.png`, and sibling `resumed-usage.cjs`.

Earlier fixture attempts failed before this check: immediate Enter after typing
was held as paste input, a second Ctrl+C targeted an already-exited window, and
the seed was not visible in the resumed inline viewport. The final fixture waits
after typing, checks for process exit, and verifies the saved transcript directly.
History exists in the transcript viewer; why it was initially absent above the
composer remains a separate rendering investigation, not established data loss.

### Default queue recall — native input coverage

The older multi-message native fixture explicitly bound Ctrl+Q, so it did not
prove the requested default-key workflow. A new isolated native VTE check using
installed CLI `ffc524ee` queues three messages with ordinary Enter during an
open response, adds a fourth draft, and presses plain Up once. All four pieces
return to the composer and the queue is cleared. The screenshot was inspected.
Editing the recalled text, pressing Enter to queue it, then empty Enter to send
interrupts the active response and delivers every piece exactly once in the
next provider request. Two controlled local requests were made; no paid calls.
Evidence: `.tmp/final-candidate/default-queue-recall-installed/result.json`,
`recalled.png`, and the sibling `default-queue-recall.cjs` fixture.
No production changes were necessary. This closes the default-key engineering
coverage gap, not Masih's actual-terminal acceptance or the separate resumed
`/usage` and physical two-finger gesture checks.

### Live theme transition and installed-process follow-up

The installed `ffc524ee` CLI passed a hidden native VTE dark → light → dark
check with zero provider requests. The fixture changes terminal colors and
injects a focus-in event, exercising palette re-query in the running TUI.
The composer, Vim Insert footer and live Context Ledger remain readable;
the draft survives both transitions and accepts further typing. Captures and
result: `.tmp/final-candidate/theme-transition-installed/`.
This does not test an actual operating-system theme notification, streaming,
or every animation phase. Historical scrollback retains previously emitted
colors; this check does not establish recoloring of the whole transcript.

A fresh read-only process check still finds PIDs 12575, 13226 and 14851 mapped
to deleted `ba9d7704` CLI executables, with OS cwd `/home/masih/Desktop/p`.
The shell installation is `ffc524ee`; the normal shared socket is absent.
Existing processes therefore still do not exercise the installed fixes.
No user session was stopped.

The current installed CLI and extension 0.1.29 also passed a fresh ordinary
startup check: IDE History discovers the CLI thread, attaches live editor tools,
and sends a reply visible in the still-running terminal. The native VS Code
run passed all 21 checks, including simultaneous history, messages in both
directions, interruption and unsaved editor tools. Its light-mode screenshot
was inspected. All provider traffic used the controlled local fixture.
Evidence: `.tmp/final-candidate/footer-latest-auto-shared.log`,
`footer-latest-native-shared-direct.log`, and
`editors/vscode/.test-data/run-1789349334226/result.json`.
The first native launcher attempt exited before tests through the Snap wrapper;
using the installed Electron executable directly completed the check. Neither
that launcher failure nor successful isolated checks establish acceptance in
Masih's existing editor window. Start fresh `elpis` in the exact editor project
folder, reload VS Code, and select that conversation in History. `/ide status`
is entered in the terminal CLI, not the extension's message composer.
Extension 0.1.29 remains installed. No user process was restarted or remote
release published. Memory policy and semantic reliability remain unresolved.
